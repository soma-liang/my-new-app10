import * as React from "react";
import * as ReactDOM from "react-dom";
import TemperatureMointor from "./temperatureMointor";
function render() {
  ReactDOM.render(<TemperatureMointor />, document.body);
}
render();
